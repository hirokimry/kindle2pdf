# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/

# 実行成果物・作業ディレクトリ（撮影生画像/確定ページ/OCR/PDF）
work/
config.yaml
state.json

# Node（npx フロント。依存は publish 時に解決、CI テストは @clack 非依存で node --test）
node_modules/

# OS
.DS_Store

# fixtures の大容量バイナリ（PoC実画像はローカル配置。Git LFS 運用にするなら別途）
tests/fixtures/*.png
tests/fixtures/*.pdf
